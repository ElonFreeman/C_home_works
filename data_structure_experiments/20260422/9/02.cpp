#include<bits/stdc++.h>
using namespace std;

struct parenth
{
    int top=-1;
    vector<char> str_left; 
};

void check(string str)
{
    parenth paren;
    while(size(paren.str_left))
    {
        
    }

}

int main(void)
{
    string str; cin >> str;
    check(str);

    return 0;
}